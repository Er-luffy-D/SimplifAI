## 🔧 Environment Variables (`.env`)
## Copy and rename the provided `.env.example` to `.env`, then fill in the values:

# 🔐 AI Configuration
# These values are required to connect with OpenRouter or DeepSeek for AI-powered responses.
AI_API_KEY="your_openrouter_or_deepseek_api_key"  # API key from OpenRouter
NEXT_PUBLIC_AI_URL="https://openrouter.ai/api/v1/chat/completions" # Base URL for chat completions
NEXT_PUBLIC_BACKEND_URL="http://localhost:3000" # Your backend server URL

# 🔐 Auth Configuration
# Used for user authentication via Google OAuth and session encryption.
AUTH_SECRET="your_random_auth_secret" # Can be generated using openssl (e.g., openssl rand -base64 32)
GOOGLE_CLIENT_ID="your_google_oauth_client_id" # Google OAuth Client ID
GOOGLE_CLIENT_SECRET="your_google_oauth_client_secret" # Google OAuth Client Secret

# 🗄️ Database
# Your Prisma connection strings for database access and connection pooling.
# Transaction pooler
DATABASE_URL="postgresql://username:password@host:port/dbname" # Main connection string for Prisma client
# Session pooler
DIRECT_URL="postgresql://username:password@host:port/dbname" # Used by Prisma for direct database access    

# 🔑 Other Secrets (omnidim chatbot)
NEXT_PUBLIC_OMNIDIM_SECRET_KEY="your_public_secret_key"  # Secret key used by the Omnidim chatbot
